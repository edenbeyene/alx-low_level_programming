#include <stdio.h>

/**
 *main - function return value
 *@argc: array size argv
 *@argv: the array of size argc
 *
 * Return: nothing
 */
int main(int __attribute__((__unused__)) argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
